---
agent: agent
description: Write API tests using Playwright APIRequestContext for REST endpoints
---

Your task is to write API tests using Playwright `APIRequestContext`.

Read the full instructions from the skill file before proceeding:

```
#file:.github/skills/api-testing/SKILL.md
```

The user will provide the API endpoint(s), spec, or curl examples. Follow the skill's procedure step-by-step: gather prerequisites, plan test cases, implement tests, and verify they pass.
